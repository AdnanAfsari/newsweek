$(document).ready(function() {
	"use strict";


	/*
	|----------------------------------------------------------------------------
	| STICKY NAVBAR
	|----------------------------------------------------------------------------
	*/
	// if ( matchMedia( 'only screen and (min-width: 768px)' ).matches ) {
	// $(document).on('scroll', function() {
	// var scrollPos = $(this).scrollTop();
  //
	// if( scrollPos > 150 ) {
	// $('.fixed-top').removeClass('.my-top-nav');
	// } else {
	// $('fixed-top').addClass('.my-top-nav');
	// }
	// });


	}
